pub mod render {
    pub struct Program {
        pid: u32,
        vertex_shader: u32,
        fragment_shader: u32,
    }
    impl Program {
        pub fn gl_use() {
            unsafe {

            }
        }
    }
}